今天真的是太倒霉了!
早上出门晚了,三辆共享单车都没法骑
第一辆:开不了锁
第二辆:链子脱了
第三辆:二维码被刮花了
电单车第一辆:没电
电单车第二辆:二维码被刮花了
还好走了一半的路程遇到一辆可以骑的车

今天安装了新的phpstudy 然后忘了备份数据 ~~~~~~~~~~~~~~~~~~
wamp的PDO用不了 搜了半天也没收到答案
头一次用宝塔,改了用户名和密码,结果改的是linux的用户名密码
今天了解了linux下 装软件的方式,搞不明白弄这么多种干嘛,不能用一种格式吗
|--> 主动连接         <----发出收到回执|
|                     <---发出被动链接 |
|---->确认回执        ------>链接成功  |


|---->主动断开                         |         
|                         <----发出回执|
|                        <-----被动断开|
|---->确认回执(time wait)          断开|
|断开

"\n"和'\n'不一样
'\n'是char类型的值   单个字符
"\n"是字符串类型的值  正好是单个字符,不能存在char类型里面
getElementsByName  老忘了elements的s
select option:seleted .val() 判断是否选中下拉菜单选项
animated shake 摇晃
给背景加渐变色 background-image:linear-gradient(left,red,blue)
然后把背景选中在字体 -webkit-background-clip :text
最后把 设置字体为透明 color:transparent
text-fill-color:transparent

还有一种方法是:遮罩 不知道是不是和会声会影里面的遮罩是不是一样的

指针数组 是数组
数组指针 是指针
int* p  int*类型指针    初始化 nullptr 或者 null 0 
int *p int类型 *p变量    void*  任意类型  但是不能操作OA
int &p 引用
int& p


糊涂的很,
void(*p)();//指针变量p指向一个函数,这个函数的返回类型和参数类型都是void
*(int*)&p = (int)func;//(int*)&p表示把指针变量本身的地址强制装换成指向int类型的指针
(*p)();
(int*)&p表示取出这个指针里面的值,指针p保存的值(int)func表示将函数的入口地址转换成int类型的值
综上 *(int*)&p = (int)func  就是将 func的地址转换成int类型 然后 给 被转换成int类型的p指针
然后p保存的内容就是函数的int类型的地址 然后*地址就是调用函数


(int) &p  表示将变量p本身的地址强制转化成int类型的指针
(int*)&p表示取出这个指针保存的值
搞不清为啥要用p本身的地址指向p的内容
而且p的地址不就是指向p本身的内容吗
(int )&p   和(int *)&p
为啥不能写成强制类型转换p为int类型不就好了吗


糊涂了
今天先回去了 ~~~~~~
今天遇到 object.setAttribute('onclick','check(this);return false;');
太恶心了,一直在想函数里面的return false,只退出当前的函数.但是没有退出object的动作,所以要在check(this);return false;
一直以为check后面不能加东西,好事too young too simple啊
还以为是作用域的问题
试了各种封闭作用域,直觉就说应该是object的动作没有暂停.多看多写.
