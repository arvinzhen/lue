今天用了xshell,下载页面真的搞人心态.
还有就是php的声明创建对象,有点像指针啊,变量存地址,地址指向对象,栈板,堆
还有就是如果把 一个对象作为实参,这个对象的地址会被传进去,而且在函数里面还有new对象的话,还得为这个象重新在堆上面分配内存,并且以形参作为地址存储变量,所以就有两个对象
检查进程ps -ef |grep php等方法。
rpm安装检查：rpm -qa |grep php
yum安装检查：yum list installed |grep php